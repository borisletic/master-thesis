# Runtime deps for the over-refusal evaluation pipeline.
#
# The pipeline is intentionally lean: inference + classification + metrics +
# aggregation all use only the Python standard library plus `requests` (for the
# Ollama HTTP API). This keeps it runnable on any Python 3.11+ (incl. 3.14, where
# some scientific wheels are not yet available). No pandas / numpy / scikit-learn.

# --- core (required) ---
requests>=2.31

# --- dev / test ---
pytest>=7.4
