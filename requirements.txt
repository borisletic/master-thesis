# Core runtime deps for the over-refusal evaluation pipeline.
#
# NOTE on Python version: this repo is developed with Python 3.14, but several
# scientific packages (pandas, scikit-learn) may not yet ship wheels for 3.14.
# Python 3.11/3.12 is the safest target. The inference + classification core
# depends only on `requests` + `pyyaml` (pure-Python) so it runs anywhere; the
# heavier analysis deps are isolated and only needed by scripts/analyze_results.py.

# --- core (always required) ---
requests>=2.31
PyYAML>=6.0

# --- analysis (scripts/analyze_results.py) ---
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tabulate>=0.9

# --- dev / test ---
pytest>=7.4
